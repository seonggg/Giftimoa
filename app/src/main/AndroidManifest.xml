<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mygifty">
    <!-- 읽기 권한 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyGifty">
        <activity android:name=".Info"
            android:theme="@style/Theme.Regist"></activity>
        <activity
            android:name=".RegistActivity"
            android:theme="@style/Theme.Regist"></activity>
        <activity
            android:name=".EditActivity"
            android:theme="@style/Theme.Regist"></activity>

        <activity android:name=".MainActivity">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Setting"
            android:theme="@style/Theme.MyGifty"></activity>

        <activity
            android:name=".SettingTime"
            android:theme="@style/Theme.MyGifty"></activity>
        <receiver android:name=".fragment.AlarmRecevier"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>